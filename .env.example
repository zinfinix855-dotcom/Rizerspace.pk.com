# Server
PORT=4000

# Frontend URL (for CORS — change to your deployed URL in production)
FRONTEND_URL=http://localhost:3000

# Database (when you connect a real DB — e.g. PostgreSQL via Prisma)
# DATABASE_URL=postgresql://user:password@localhost:5432/kaizen_store

# Stripe (for real payments)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Email (for order confirmations)
# RESEND_API_KEY=re_...
# FROM_EMAIL=orders@kaizenfigures.com
